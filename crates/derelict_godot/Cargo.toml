[package]
name = "derelict_godot"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
derelict_core = { path = "../derelict_core" }
godot = "0.5"
serde_json = "1"

[dev-dependencies]
blake3 = "1"
serde_json = "1"
